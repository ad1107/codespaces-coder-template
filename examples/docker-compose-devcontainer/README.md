# Docker Compose Devcontainer Example

This example is meant for the v2 Dev Containers Integration template.

Verify the app container:

```bash
cat /workspaces/docker-compose-devcontainer/compose-ready.txt
```
